{% extends 'base.html.twig' %}

{% block body %}
    <h1>AllegroComparator</h1>

    <p>Hello World!</p>

    <article>
        <h2>VueJS test</h2>

        <div id="demo">
            <form id="search">
                Search
                <input
                    name="query"
                    v-model="searchQuery"
                />
            </form>
            <grid
                :data="gridData"
                :columns="gridColumns"
                :filter-key="searchQuery"
            />
        </div>
    </article>
{% endblock %}
